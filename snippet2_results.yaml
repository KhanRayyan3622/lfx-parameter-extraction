architectural_parameters: []

note: "No true architectural parameters identified in this snippet"

rationale:
  - The 12-bit CSR encoding space (csr[11:0]) is a STANDARD ISA CONVENTION fixed across all RISC-V implementations, not a parameter that varies between implementations.
  - The bit allocation scheme (csr[11:8] for access control encoding) is a STANDARD CONVENTION mandated by the ISA specification.
  - The read/write encoding in csr[11:10] (00, 01, 10 = read/write; 11 = read-only) is a STANDARD CONVENTION, not a parameter.
  - The privilege level encoding in csr[9:8] is a STANDARD CONVENTION, not a parameter.

what_could_be_parameters_but_not_specified_here:
  - csr_implementation_set
    description: >
      Which specific CSRs (by address index) are actually implemented in a
      particular RISC-V core is implementation-specific, but this snippet does
      not enumerate or parameterize this.
    type: array
    confidence: LOW
    note: "Not extractable from this snippet"

conclusion: >
  This snippet describes the standard CSR addressing and access control scheme
  that applies uniformly to all RISC-V implementations. It establishes conventions,
  not parameters. True CSR-related parameters (e.g., which CSRs exist, their
  default values, supported features) would be found elsewhere in the specification
  or in implementation-specific documentation.
